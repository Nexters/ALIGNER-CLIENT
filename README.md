# ALIGNER-CLIENT

폴더 구조와 작업 파이프라인은 [project_guidelines.md](./project_guidelines.md) 참고.

## 시작하기

```bash
pnpm install
cp .env.example .env.local
pnpm dev
```

- `pnpm build` — 타입체크 + 프로덕션 빌드
- `pnpm typecheck` — 타입체크만
- `pnpm lint` — ESLint
- `pnpm format` — Prettier
